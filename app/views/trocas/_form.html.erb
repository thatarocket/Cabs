<%= form_with(model: troca) do |form| %>
  <% if troca.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(troca.errors.count, "error") %> prohibited this troca from being saved:</h2>

      <ul>
        <% troca.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.hidden_field :proposta_por_id, value: Current.usuario.username %>
  </div>

  <div>
    <%= form.label :proposta_para_id, style: "display: block" %>
    <%= form.text_field :proposta_para_id %>
  </div>

  <div>
    <%= form.label :item_proposto_id, style: "display: block" %>
    <%= form.text_field :item_proposto_id %>
  </div>

  <div>
    <%= form.label :item_id, style: "display: block" %>
    <%= form.text_field :item_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
